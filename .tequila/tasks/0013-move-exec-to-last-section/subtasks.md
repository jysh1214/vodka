# Subtasks of *Move Exec to Last Section*

## Update ordering rules

- [x] Update `SKILL.md` Rules section: change `system → pyvenv → dep/deps → all others` to `system → pyvenv → dep/deps → all others → exec`
- [x] Update `references/reproduce.md` Priority Order: split `exec` out of "All other categories" (item 3) into its own final step (item 4)
- [x] Update `references/reproduce.md` Steps: add a new `### 4. exec/*` section and remove `exec` from the "Other categories" section
