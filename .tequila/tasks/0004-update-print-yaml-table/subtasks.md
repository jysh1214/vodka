# Subtasks of *Update Print YAML Table*

## Implementation

- [x] Rewrite `print_table` in `scripts/print_yaml.py` to use box-drawing characters (`┌ ┬ ┐ ├ ┼ ┤ └ ┴ ┘ │ ─`) with row separators between every row
- [x] Run `python3 scripts/print_yaml.py` and verify the output matches the expected box-drawing format
